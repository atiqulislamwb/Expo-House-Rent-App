interface AppStyles {
  primaryColor: string;
  headingColor: string;
  backgroundColor: string;
  subHeadingColor: string;
  inactiveIconColor: string;
}

const color: AppStyles = {
  primaryColor: '#33CAD3',
  headingColor: '#142F4B',
  backgroundColor: '#F7FAFF',
  subHeadingColor: '#9A9EA1',
  inactiveIconColor: '#C8CDD2',
};

export default color;